---
title: "Vorto: a Vim-style terminal editor that just works"
description: "A Rust-based modal terminal text editor with built-in LSP, tree-sitter highlighting, and GitHub Copilot — no plugin manager, no init.lua."
---

<Frame>
  <img src="/vorto.png" alt="Vorto" />
  Vorto
</Frame>

Vorto is a Rust-based modal terminal editor that bundles an LSP client, tree-sitter syntax highlighting, and GitHub Copilot support (through the Copilot LSP). Grammars install on demand, and the entire configuration lives in a single TOML file — no plugin manager, no init.lua, no package.json.

## Why Vorto

In the AI era, humans write less code by hand than ever before. Most of what we still do in an editor is read, review, and surgically edit — the exact workload modal editing was built for. Vim's command grammar is as relevant as it has ever been.

What has stopped being worth it is the editor itself. Curating a plugin list, pinning versions, debugging a broken `init.lua` after an upgrade, wiring up LSP and tree-sitter and a completion engine by hand — that maintenance tax made sense when the editor was where you spent your day. It does not make sense when the editor is one tool among many in an AI-assisted workflow.

Vorto is the response to that shift:

- **Batteries included.** LSP client, tree-sitter highlighting, fuzzy finder, and GitHub Copilot all ship in the binary. Install once with `cargo install vorto` and start editing — no plugin manager, no `init.lua`, no `package.json`.
- **Vim commands, lower learning cost.** Vorto follows Vim's modal model, and surfaces [command hints inline](/editing/hints) so you don't have to keep the full keymap in your head. Pick it up faster; come back to it after a month without losing the muscle memory.
- **One config file.** Keybindings, themes, indentation, and per-language settings all live in a single TOML file. Nothing to bootstrap, nothing to migrate.

## Core concepts

**Modal editing.** Five modes (Normal, Insert, Visual, Visual Line, Visual Block) plus a `:` command prompt. The status bar always shows the active mode. See [Editing modes](/editing/modes).

**Language support.** Tree-sitter grammars install on demand via `vorto grammar install`. 27 languages ship with built-in recipes. See [Supported languages](/languages/supported-languages).

**LSP client.** Diagnostics, completions, hover, code actions, and goto definition out of the box. Servers spawn lazily when you open a file. See [LSP overview](/lsp/overview).

**Configuration.** A single TOML file at `~/.config/vorto/config.toml` controls editor settings, keymaps, languages, and LSP servers. See [Configuration overview](/configuration/overview).

## Explore the docs

<CardGroup cols={2}>
  <Card title="Installation" icon="download" href="/installation">
    Install Vorto from crates.io or build from source in minutes.
  </Card>
  <Card title="Quick start" icon="rocket" href="/quickstart">
    Open your first file, navigate in Normal mode, and start editing.
  </Card>
  <Card title="Editing modes" icon="keyboard" href="/editing/modes">
    Understand Normal, Insert, Visual, and Command mode in depth.
  </Card>
  <Card title="Language grammars" icon="tree" href="/languages/grammars">
    Install and manage tree-sitter grammars for syntax highlighting.
  </Card>
  <Card title="LSP client" icon="plug" href="/lsp/overview">
    Connect language servers for completions, diagnostics, and more.
  </Card>
  <Card title="Configuration" icon="sliders" href="/configuration/overview">
    Customize keybindings, themes, and language settings via TOML.
  </Card>
</CardGroup>
